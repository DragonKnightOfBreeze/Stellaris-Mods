greetings = {
	# 故事传述者
	SR_HOSTILE_GREETING_01 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_at_war_with = FROM }
			OR = {
				is_hostile_to = FROM
				is_unfriendly_to = FROM
			}
		}
	}
	SR_HOSTILE_GREETING_02 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_same_species = FROM }
			NOT = { is_at_war_with = FROM }
			OR = {
				is_hostile_to = FROM
				is_unfriendly_to = FROM
			}
			FROM = {
				NOT = {
					has_authority = auth_machine_intelligence
					is_species_class = ROBOT
				}
			}
		}
	}
	SR_HOSTILE_GREETING_03 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			FROM = {
				OR = {
					has_ethic = ethic_spiritualist
					has_ethic = ethic_fanatic_spiritualist
				}
			}
			NOT = { is_same_species = FROM }
			NOT = { is_at_war_with = FROM }
			OR = {
				is_hostile_to = FROM
				is_unfriendly_to = FROM
			}
		}
	}
	SR_HOSTILE_GREETING_04 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			FROM = {
				OR = {
					has_ethic = ethic_authoritarian
					has_ethic = ethic_fanatic_authoritarian
				}
			}
			NOT = { is_same_species = FROM }
			NOT = { is_at_war_with = FROM }
			OR = {
				is_hostile_to = FROM
				is_unfriendly_to = FROM
			}
			FROM = {
				NOT = {
					has_authority = auth_machine_intelligence
					is_species_class = ROBOT
				}
			}
		}
	}
	SR_DOMINEERING_GREETING_01 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_at_war_with = FROM }
			OR = {
				is_domineering_to = FROM
			}
		}
	}
	SR_NEUTRAL_GREETING_01 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_at_war_with = FROM }
			is_neutral_to = FROM
		}
	}
	SR_NEUTRAL_GREETING_02 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_at_war_with = FROM }
			is_neutral_to = FROM
		}
	}
	SR_NEUTRAL_GREETING_03 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			FROM = {
				OR = {
					has_ethic = ethic_spiritualist
					has_ethic = ethic_fanatic_spiritualist
				}
			}
			NOT = { is_same_species = FROM }
			NOT = { is_at_war_with = FROM }
			is_neutral_to = FROM
		}
	}
	SR_THREATENED_GREETING_01 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_at_war_with = FROM }
			is_threatened_to = FROM
		}
	}
	SR_FRIENDLY_GREETING_01 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_at_war_with = FROM }
			OR = {
				is_friendly_to = FROM
				is_cordial_to = FROM
				is_protective_to = FROM
			}
		}
	}
	SR_FRIENDLY_GREETING_02 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_same_species = FROM }
			NOT = { is_at_war_with = FROM }
			OR = {
				is_friendly_to = FROM
				is_cordial_to = FROM
				is_protective_to = FROM
			}
		}
	}
	SR_FRIENDLY_GREETING_03 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			FROM = {
				OR = {
					has_ethic = ethic_materialist
					has_ethic = ethic_fanatic_materialist
				}
			}
			NOT = { is_same_species = FROM }
			NOT = { is_at_war_with = FROM }
			OR = {
				is_friendly_to = FROM
				is_cordial_to = FROM
				is_protective_to = FROM
			}
		}
	}
	SR_FRIENDLY_GREETING_04 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			is_in_federation_with = FROM
			NOT = { is_at_war_with = FROM }
			OR = {
				is_friendly_to = FROM
				is_cordial_to = FROM
				is_protective_to = FROM
			}
		}
	}
	SR_WAR_GREETING_01 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			is_at_war_with = FROM
		}
	}
	SR_WAR_GREETING_02 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			NOT = { is_same_species = FROM }
			is_at_war_with = FROM
		}
	}
	SR_WAR_GREETING_03 = {
		weight = {
			weight = 3
		}
		trigger = {
			has_ai_personality = story_retellers
			FROM = {
				OR = {
					has_ethic = ethic_spiritualist
					has_ethic = ethic_fanatic_spiritualist
				}
			}
			NOT = { is_same_species = FROM }
			is_at_war_with = FROM
		}
	}
	SR_VASSAL_GREETING_01 = {
		weight = {
			weight = 10
		}
		trigger = {
			has_ai_personality = story_retellers
			exists = overlord
			overlord = { is_same_value = FROM }
			NOT = { is_at_war_with = FROM }
			NOT = { is_disloyal_to = FROM }
		}
	}
	SR_VASSAL_GREETING_DISLOYAL_01 = {
		weight = {
			weight = 10
		}
		trigger = {
			has_ai_personality = story_retellers
			exists = overlord
			overlord = { is_same_value = FROM }
			NOT = { is_at_war_with = FROM }
			is_disloyal_to = FROM
		}
	}
}

action_declare_war = {
	propose = {
		# 故事传述者
		08_DOW_PROPOSE = {
			weight = {
				weight = 5
			}
			trigger = {
				FROM = {
					has_ai_personality = story_retellers
					NOT = {
						OR = {
							has_ethic = ethic_xenophobe
							has_ethic = ethic_fanatic_xenophobe
							has_authority = auth_corporate
						}
					}
				}
			}
		}
	}
}